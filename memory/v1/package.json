{
  "name": "@coasys/social-dna-memory-v1",
  "version": "1.0.0",
  "description": "MemoryEntry social DNA for AD4M — AI agent persistent distributed memory",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "MemoryEntry.json"
  ],
  "scripts": {
    "build:shacl": "tsx ../../scripts/build-shacl.ts MemoryEntry.ts MemoryEntry.json",
    "build:ts": "tsc",
    "build": "npm run build:shacl && npm run build:ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@coasys/ad4m": "0.12.1-dev"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "license": "MIT"
}
